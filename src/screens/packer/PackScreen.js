import React from 'react';
import { SafeAreaView, Text } from 'react-native';


const PackScreen = () => {
    return (
        <SafeAreaView>
            <Text>Nesto: PackScreen</Text>
        </SafeAreaView>
    );
};

export default PackScreen;