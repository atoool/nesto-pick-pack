import React from 'react';
import { SafeAreaView, Text } from 'react-native';

const PackScreen = () => {
  return (
    <SafeAreaView>
      <Text>Pack: PackScreen</Text>
    </SafeAreaView>
  );
};

export default PackScreen;
